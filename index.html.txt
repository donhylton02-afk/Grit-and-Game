

<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <meta name="color-scheme" content="dark light" />
  <title>Grit &amp; Game — A Code</title>
  <meta name="description" content="Grit & Game is a code: carry yourself correctly no matter what. Quiet standards. Relentless reps." />

  <!-- Open Graph (for sharing) -->
  <meta property="og:title" content="Grit &amp; Game — A Code" />
  <meta property="og:description" content="Carry yourself correctly no matter what. Quiet standards. Relentless reps." />
  <meta property="og:type" content="website" />

  <style>
    :root{
      /* Midnight Discipline */
      --bg: #070A10;
      --panel: rgba(255,255,255,.045);
      --panel2: rgba(255,255,255,.065);
      --text: rgba(255,255,255,.92);
      --muted: rgba(255,255,255,.68);
      --faint: rgba(255,255,255,.48);
      --line: rgba(255,255,255,.12);
      --accent: #3EA6FF; /* electric blue */
      --accent2: #9BB3C8; /* gunmetal-ish */
      --shadow: 0 24px 80px rgba(0,0,0,.55);
      --radius: 18px;
      --max: 1040px;

      --mono: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
      --sans: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji", "Segoe UI Emoji";
    }

    /* Optional light mode (kept calm) */
    @media (prefers-color-scheme: light){
      :root{
        --bg: #F6F8FB;
        --panel: rgba(0,0,0,.035);
        --panel2: rgba(0,0,0,.055);
        --text: rgba(6,10,16,.90);
        --muted: rgba(6,10,16,.62);
        --faint: rgba(6,10,16,.48);
        --line: rgba(6,10,16,.12);
        --shadow: 0 24px 80px rgba(0,0,0,.12);
      }
    }

    *{ box-sizing: border-box; }
    html,body{ height: 100%; }
    body{
      margin: 0;
      font-family: var(--sans);
      color: var(--text);
      background: radial-gradient(1200px 700px at 25% 0%, rgba(62,166,255,.12), transparent 55%),
                  radial-gradient(900px 600px at 80% 25%, rgba(155,179,200,.10), transparent 60%),
                  var(--bg);
      letter-spacing: .2px;
      line-height: 1.45;
    }

    a{ color: inherit; text-decoration: none; }
    a:hover{ text-decoration: underline; text-decoration-color: rgba(62,166,255,.6); }

    .wrap{
      max-width: var(--max);
      margin: 0 auto;
      padding: 32px 20px 72px;
    }

    header{
      position: sticky;
      top: 0;
      z-index: 30;
      backdrop-filter: blur(10px);
      background: linear-gradient(to bottom, rgba(0,0,0,.35), rgba(0,0,0,0));
      border-bottom: 1px solid rgba(255,255,255,.06);
    }
    @media (prefers-color-scheme: light){
      header{
        background: linear-gradient(to bottom, rgba(246,248,251,.72), rgba(246,248,251,0));
        border-bottom: 1px solid rgba(0,0,0,.06);
      }
    }

    .nav{
      max-width: var(--max);
      margin: 0 auto;
      padding: 14px 20px;
      display: flex;
      align-items: center;
      gap: 14px;
      justify-content: space-between;
    }

    .brand{
      display: flex;
      align-items: baseline;
      gap: 10px;
      white-space: nowrap;
    }
    .brand .mark{
      font-family: var(--mono);
      font-size: 12px;
      color: var(--accent);
      letter-spacing: 1.4px;
      text-transform: uppercase;
    }
    .brand .name{
      font-weight: 650;
      letter-spacing: .6px;
    }

    nav a{
      font-size: 14px;
      color: var(--muted);
      padding: 8px 10px;
      border-radius: 10px;
    }
    nav a:hover{
      background: var(--panel);
      text-decoration: none;
      color: var(--text);
    }

    .nav-right{
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .btn{
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      padding: 10px 14px;
      border-radius: 12px;
      border: 1px solid var(--line);
      background: var(--panel);
      box-shadow: none;
      font-size: 14px;
      color: var(--text);
      cursor: pointer;
      user-select: none;
    }
    .btn:hover{
      background: var(--panel2);
      text-decoration: none;
    }
    .btn.primary{
      border-color: rgba(62,166,255,.35);
      background: linear-gradient(180deg, rgba(62,166,255,.18), rgba(62,166,255,.08));
    }
    .btn.primary:hover{
      background: linear-gradient(180deg, rgba(62,166,255,.24), rgba(62,166,255,.10));
    }
    .btn.small{ padding: 9px 12px; font-size: 13px; }

    .hero{
      padding: 64px 0 26px;
      display: grid;
      grid-template-columns: 1.2fr .8fr;
      gap: 26px;
      align-items: start;
    }
    @media (max-width: 860px){
      .hero{ grid-template-columns: 1fr; padding-top: 42px; }
    }

    .card{
      border: 1px solid var(--line);
      background: var(--panel);
      border-radius: var(--radius);
      box-shadow: var(--shadow);
    }

    .hero .left{
      padding: 28px 26px;
    }
    .hero .right{
      padding: 22px 22px;
    }

    .kicker{
      font-family: var(--mono);
      font-size: 12px;
      letter-spacing: 1.6px;
      text-transform: uppercase;
      color: var(--accent);
      margin: 0 0 10px;
    }

    h1{
      margin: 0 0 14px;
      font-size: clamp(34px, 4.2vw, 54px);
      letter-spacing: .2px;
      line-height: 1.06;
    }
    .sub{
      margin: 0 0 18px;
      color: var(--muted);
      font-size: 16px;
      max-width: 56ch;
    }

    .hero-actions{
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-top: 16px;
    }

    .quote{
      padding: 18px 18px 16px;
      border-radius: 16px;
      border: 1px solid rgba(62,166,255,.22);
      background: linear-gradient(180deg, rgba(62,166,255,.12), rgba(62,166,255,.04));
      margin-bottom: 14px;
    }
    .quote p{ margin: 0; font-size: 14px; color: var(--text); }
    .quote .by{
      margin-top: 10px;
      font-family: var(--mono);
      color: var(--faint);
      font-size: 12px;
      letter-spacing: 1.1px;
      text-transform: uppercase;
    }

    .mini{
      display: grid;
      gap: 10px;
    }
    .mini .row{
      padding: 12px 12px;
      border-radius: 14px;
      border: 1px solid var(--line);
      background: rgba(255,255,255,.03);
    }
    .mini .label{
      font-family: var(--mono);
      font-size: 11px;
      letter-spacing: 1.4px;
      text-transform: uppercase;
      color: var(--faint);
      margin: 0 0 4px;
    }
    .mini .val{
      margin: 0;
      color: var(--text);
      font-size: 14px;
    }

    section{
      margin-top: 18px;
      padding: 26px 0 0;
    }

    .section-head{
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      gap: 14px;
      margin: 0 0 12px;
    }
    .section-head h2{
      margin: 0;
      font-size: 18px;
      letter-spacing: .6px;
    }
    .section-head .hint{
      color: var(--muted);
      font-size: 13px;
    }

    .grid{
      display: grid;
      grid-template-columns: repeat(12, 1fr);
      gap: 14px;
    }

    .col-6{ grid-column: span 6; }
    .col-12{ grid-column: span 12; }
    .col-4{ grid-column: span 4; }
    @media (max-width: 860px){
      .col-6,.col-4{ grid-column: span 12; }
    }

    .panel{
      padding: 18px 18px 16px;
      border: 1px solid var(--line);
      border-radius: var(--radius);
      background: var(--panel);
    }
    .panel h3{
      margin: 0 0 8px;
      font-size: 14px;
      letter-spacing: .6px;
      text-transform: uppercase;
      font-family: var(--mono);
      color: var(--accent2);
    }
    .panel p{
      margin: 0;
      color: var(--muted);
      font-size: 14px;
    }

    .code{
      list-style: none;
      margin: 12px 0 0;
      padding: 0;
      display: grid;
      gap: 10px;
    }
    .code li{
      display: grid;
      grid-template-columns: auto 1fr;
      gap: 10px;
      padding: 12px 12px;
      border-radius: 14px;
      border: 1px solid var(--line);
      background: rgba(255,255,255,.03);
    }
    .badge{
      font-family: var(--mono);
      font-size: 12px;
      letter-spacing: 1.2px;
      color: var(--accent);
      padding-top: 2px;
      white-space: nowrap;
    }
    .code strong{
      display: block;
      font-size: 14px;
      color: var(--text);
      margin-bottom: 3px;
      letter-spacing: .2px;
    }
    .code span{
      display: block;
      font-size: 13px;
      color: var(--muted);
    }

    .divider{
      height: 1px;
      background: linear-gradient(90deg, transparent, var(--line), transparent);
      margin: 28px 0 0;
    }

    footer{
      margin-top: 34px;
      padding-top: 20px;
      color: var(--faint);
      font-size: 13px;
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      justify-content: space-between;
      align-items: center;
      border-top: 1px solid rgba(255,255,255,.08);
    }
    @media (prefers-color-scheme: light){
      footer{ border-top: 1px solid rgba(0,0,0,.08); }
    }

    .links{
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
    }
    .pill{
      padding: 9px 12px;
      border-radius: 999px;
      border: 1px solid var(--line);
      background: rgba(255,255,255,.03);
      color: var(--muted);
      font-size: 13px;
    }
    .pill:hover{
      background: var(--panel2);
      color: var(--text);
      text-decoration: none;
    }

    /* Accessibility */
    .sr-only{
      position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0;
    }
    :focus-visible{
      outline: 2px solid rgba(62,166,255,.65);
      outline-offset: 3px;
      border-radius: 10px;
    }
  </style>
</head>

<body>
  <header>
    <div class="nav" role="navigation" aria-label="Primary">
      <div class="brand">
        <span class="mark">G&amp;G</span>
        <span class="name">Grit &amp; Game</span>
      </div>

      <nav aria-label="Page sections">
        <a href="#code">The Code</a>
        <a href="#practice">In Practice</a>
        <a href="#shirt">The Shirt</a>
        <a href="#connect">Connect</a>
      </nav>

      <div class="nav-right">
        <button class="btn small" id="copyLine" type="button" title="Copy the core line">
          Copy line
        </button>
        <a class="btn small primary" href="#shirt" title="Go to the shirt section">Shirt</a>
      </div>
    </div>
  </header>

  <main class="wrap">
    <section class="hero" aria-label="Hero">
      <div class="left card">
        <p class="kicker">A philosophy page</p>
        <h1>Carry yourself correctly.<br/>No matter what.</h1>
        <p class="sub">
          Grit &amp; Game is a code for grown-men: quiet standards, clean decisions, relentless reps.
          No speeches. No performance. Just how you move.
        </p>

        <div class="hero-actions">
          <!-- Replace # with your real shop link (Shopify, Etsy, etc.) -->
          <a class="btn primary" href="#" id="shopLink">Get the shirt</a>
          <a class="btn" href="#code">Read the code</a>
          <!-- Replace with your Instagram/TikTok link if you want -->
          <a class="btn" href="#" id="socialLink">Follow the work</a>
        </div>

        <div class="divider" aria-hidden="true"></div>

        <ul class="code" id="code" aria-label="The Code">
          <li>
            <div class="badge">01</div>
            <div>
              <strong>Discipline isn’t a mood.</strong>
              <span>It’s identity. You do it because that’s who you are.</span>
            </div>
          </li>
          <li>
            <div class="badge">02</div>
            <div>
              <strong>Earned. Not announced.</strong>
              <span>No captions for validation. The receipts live in your routine.</span>
            </div>
          </li>
          <li>
            <div class="badge">03</div>
            <div>
              <strong>Calm is not softness.</strong>
              <span>Composure is a weapon. Noise is for the insecure.</span>
            </div>
          </li>
          <li>
            <div class="badge">04</div>
            <div>
              <strong>Standards over status.</strong>
              <span>You don’t chase rooms. You bring structure to them.</span>
            </div>
          </li>
          <li>
            <div class="badge">05</div>
            <div>
              <strong>One rep at a time.</strong>
              <span>Small decisions stack. That’s the whole game.</span>
            </div>
          </li>
        </ul>
      </div>

      <aside class="right card" aria-label="Sidebar">
        <div class="quote">
          <p id="coreLine">“The room gets loud. The work stays quiet.”</p>
          <div class="by">GRIT &amp; GAME</div>
        </div>

        <div class="mini" aria-label="What this is">
          <div class="row">
            <p class="label">What this is</p>
            <p class="val">A simple code for how to live, train, and show up.</p>
          </div>
          <div class="row">
            <p class="label">What this isn’t</p>
            <p class="val">Hype. Trends. Performative motivation.</p>
          </div>
          <div class="row">
            <p class="label">The signal</p>
            <p class="val">Quiet standards. Relentless reps.</p>
          </div>
        </div>
      </aside>
    </section>

    <section id="practice" aria-label="In Practice">
      <div class="section-head">
        <h2>In Practice</h2>
        <div class="hint">What the code looks like in real life.</div>
      </div>

      <div class="grid">
        <div class="panel col-6">
          <h3>Training</h3>
          <p>
            You don’t train for the clip. You train for the moment when you’re tired and it still has to be clean.
            Control first. Speed second. Ego last.
          </p>
        </div>

        <div class="panel col-6">
          <h3>Work</h3>
          <p>
            You move with clarity. You do what you said you’d do. You leave the drama for people who need it.
          </p>
        </div>

        <div class="panel col-6">
          <h3>Family</h3>
          <p>
            Love is shown in stability. Your presence is consistent. Your word is steady. Your tone stays even.
          </p>
        </div>

        <div class="panel col-6">
          <h3>Pressure</h3>
          <p>
            When it gets loud, you get quieter. When it gets messy, you get simpler. That’s how you win without talking.
          </p>
        </div>
      </div>
    </section>

    <section id="shirt" aria-label="The Shirt">
      <div class="section-head">
        <h2>The Shirt</h2>
        <div class="hint">Not the point. Just a signal.</div>
      </div>

      <div class="grid">
        <div class="panel col-8">
          <h3>What it means</h3>
          <p>
            The shirt is for people who don’t need permission to do the work.
            It’s not a brand costume. It’s a quiet marker: “I have standards.”
          </p>
          <div class="hero-actions" style="margin-top:14px;">
            <!-- Replace href with your shop URL -->
            <a class="btn primary" href="#" id="shopLink2">Shop link</a>
            <!-- Replace href with your size guide / product details page, or remove -->
            <a class="btn" href="#" id="detailsLink">Details / size guide</a>
          </div>
          <p style="margin-top:12px;color:var(--faint);font-size:13px;">
            Tip: when your shop is ready, paste the URL into <span style="font-family:var(--mono);">shopLink</span> and <span style="font-family:var(--mono);">shopLink2</span>.
          </p>
        </div>

        <div class="panel col-4">
          <h3>Drop note</h3>
          <p>
            If you want, add a small line here about when the first run ships or how often you restock.
          </p>
          <div style="margin-top:12px;">
            <span class="pill" style="display:inline-flex;">Minimal</span>
            <span class="pill" style="display:inline-flex;">Heavy</span>
            <span class="pill" style="display:inline-flex;">Clean</span>
          </div>
        </div>
      </div>
    </section>

    <section id="connect" aria-label="Connect">
      <div class="section-head">
        <h2>Connect</h2>
        <div class="hint">Keep it simple. Keep it clean.</div>
      </div>

      <div class="grid">
        <div class="panel col-12">
          <h3>Links</h3>
          <p style="margin-bottom:12px;">
            Replace these with your real links (Instagram, TikTok, YouTube, email).
          </p>

          <div class="links">
            <!-- Replace # with your real URLs -->
            <a class="pill" href="#" id="igLink">Instagram</a>
            <a class="pill" href="#" id="ttLink">TikTok</a>
            <a class="pill" href="#" id="ytLink">YouTube</a>
            <a class="pill" href="mailto:you@example.com" id="mailLink">Email</a>
          </div>
        </div>
      </div>
    </section>

    <footer aria-label="Footer">
      <div>
        <span style="font-family:var(--mono);letter-spacing:1px;">GRIT &amp; GAME</span>
        <span aria-hidden="true"> · </span>
        <span>Quiet standards. Relentless reps.</span>
      </div>
      <div>
        <button class="btn small" type="button" id="scrollTop">Top</button>
      </div>
    </footer>
  </main>

  <script>
    // === Quick edits (paste your links here) ===
    // Replace these placeholders with your real URLs.
    const LINKS = {
      shop: "#",          // e.g., "https://yourstore.com/products/grit-game-tee"
      details: "#",       // e.g., "https://yourstore.com/pages/size-guide"
      social: "#",        // e.g., "https://www.tiktok.com/@yourhandle"
      instagram: "#",
      tiktok: "#",
      youtube: "#",
      email: "you@example.com" // shown in mailto link below
    };

    // Apply links
    const byId = (id) => document.getElementById(id);

    byId("shopLink").setAttribute("href", LINKS.shop);
    byId("shopLink2").setAttribute("href", LINKS.shop);
    byId("detailsLink").setAttribute("href", LINKS.details);
    byId("socialLink").setAttribute("href", LINKS.social);

    byId("igLink").setAttribute("href", LINKS.instagram);
    byId("ttLink").setAttribute("href", LINKS.tiktok);
    byId("ytLink").setAttribute("href", LINKS.youtube);

    const mail = `mailto:${LINKS.email}`;
    byId("mailLink").setAttribute("href", mail);
    byId("mailLink").textContent = LINKS.email;

    // Copy core line
    byId("copyLine").addEventListener("click", async () => {
      const line = document.getElementById("coreLine").textContent.replace(/[“”]/g,'"');
      try{
        await navigator.clipboard.writeText(line);
        byId("copyLine").textContent = "Copied";
        setTimeout(()=> byId("copyLine").textContent = "Copy line", 1200);
      }catch(e){
        // Fallback: select text via prompt
        window.prompt("Copy this line:", line);
      }
    });

    // Scroll to top
    byId("scrollTop").addEventListener("click", () => {
      window.scrollTo({ top: 0, behavior: "smooth" });
    });
  </script>
</body>
</html>

#HTML